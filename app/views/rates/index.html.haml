%h1 Listing rates

%table
  %thead
    %tr
      %th Rate score
      %th User
      %th Restaurant
      %th
      %th
      %th

  %tbody
    - @rates.each do |rate|
      %tr
        %td= rate.rate_score
        %td= rate.user.name
        %td= rate.restaurant.name
        %td= link_to 'Show', rate
        %td= link_to 'Edit', edit_rate_path(rate)
        %td= link_to 'Destroy', rate, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Rate', new_rate_path
